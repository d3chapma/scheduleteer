<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "HPWC Scheduler" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="fixed">
      <nav class="top-bar" data-topbar role="navigation">
        <ul class="title-area">
          <li class="name">
            <h1>
              <%= link_to 'HPWC Scheduler', root_path %>
            </h1>
          </li>
        </ul>
      </nav>
    </div>
    <ul class="breadcrumbs">
      <%= yield(:breadcrumbs) %>
    </ul>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
